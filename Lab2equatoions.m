clc;
clear all;
close all;

% syms x
% y = x^3+3*x^2 + 2*(1+x);
% z = exp(x) + x^2;
% w = log10(x)+ log(x);
% m=cos(1+x)+asin(x);

% x=5;
% y = x^3+3*x^2 + 2*(1+x);
% z = exp(x) + x^2;
% w = log10(x)+ log(x);
% m=cos(1+x)+asin(x);
% these are for to display with solved values 


% for displaying with variables 
syms x y;
z= x^3 + 3*x^2 ;
q = y^3; 
